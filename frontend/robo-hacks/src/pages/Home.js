import styles from "./Home.module.css";
import react from "react"
import { Layout, Header, Content, Footer Button, DatePicker, Space, version } from "antd";
const Home = (props) => {
  return (<Layout>
    
  </Layout>)
};

export default Home;
